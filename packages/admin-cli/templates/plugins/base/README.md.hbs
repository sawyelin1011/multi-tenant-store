# {{name}}

{{description}}

## Getting started

```bash
cd {{slug}}
npm install
npm run dev
```

## Available scripts

- `npm run dev` - watch TypeScript and emit declaration files
- `npm run build` - compile the plugin for production use
- `npm run test` - execute the default vitest suite

## Next steps

- Configure `plugin.json` with correct metadata
- Build shadcn admin components under `src/admin`
- Register new hooks inside `src/hooks`
- Deploy with `mtc-admin plugin publish:prepare {{slug}}`
